//document.addEventListener("DOMContentLoaded", () => {
    //  hämta lagrad film-ID och fetchedMovies array från session storage
  //  const selectedMovieId = sessionStorage.getItem('selectedMovieId');
    //const fetchedMovies = JSON.parse(sessionStorage.getItem('fetchedMovies'));

    // hitta den valda filmen från fetchedMovies arrayen
    //const selectedMovie = fetchedMovies.find(movie => movie.imdbID === selectedMovieId);

    // Get elements for displaying movie details
    //const movieTitleElement = document.getElementById('movieTitle');
    //const moviePosterElement = document.getElementById('moviePoster');
    //const movieYearElement = document.getElementById('movieYear');
    //const moviePlotElement = document.getElementById('moviePlot');

    //  visa filmens detaljer
    //if (selectedMovie) {
      //  movieTitleElement.textContent = selectedMovie.Title;
        //moviePosterElement.src = selectedMovie.Poster;
        //movieYearElement.textContent = `Year: ${selectedMovie.Year}`;
        //moviePlotElement.textContent = `Plot: ${selectedMovie.Plot}`;
    //} else {
        //  fallet där den valda filmen inte hittas
      //  console.error('Film hittades inte.');
    //}
//});
